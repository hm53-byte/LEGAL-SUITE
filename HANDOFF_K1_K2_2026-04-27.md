# HANDOFF — kraj sesije 2026-04-27 → start sljedeće sesije

> Napisano: 2026-04-27 popodne. Sljedeća sesija: korisnik nastavlja s K1.5 (UI ožičenje stranice/*.py s novim `pripremi_za_docx` potpisom), zatim K2 (PWA Service Worker, P2 hibrid ciklus).

---

## Što je danas isporučeno

### K5 — Vodič refactor u passive katalog (zatvoreno)

- `LEGAL-SUITE.py:_VODIC_KATEGORIJE` (10 problema u 1. licu) → `_PODRUCJA_KATALOG` (11 područja, objektivna taksonomija)
- `_VODIC_DETALJI` (savjetodavne upute s rok-žurnost) → `_PODRUCJA_DETALJI` (faktička enumeracija dokumenata)
- Disclaimer banner u `_render_pocetna()` s HOK Imenik linkom
- Hero "Odaberite situaciju" → "Odaberite modul iz kataloga"
- "Pokazi korake" → "Vidi tipove dokumenata"
- Backwards-compat aliases zadržani
- **Razlog**: trenutni Vodič je bio implicit klasifikator pravnog problema → rub nadripisarstva (ZO čl. 72) + AI Act Annex III pt. 8 visok rizik. Refactor vraća app u out-of-scope poziciju.
- **Test status**: 159/159 PASS, 0 regresija.

### K3 — Cloud-native monetizacija (zatvoreno A/B/D inline)

Stack 24/7 čak kad je razvojni PC isključen:
- `cloud/supabase_schema.sql` (140 LOC) — 4 tablice (`users`, `entitlements`, `download_log`, `stripe_events`) + Row-Level Security
- `cloud/cf_worker_stripe.ts` (~180 LOC) — Cloudflare Worker s Stripe webhook + checkout endpoint
- `cloud/wrangler.toml` + `cloud/package.json` — deploy config
- `cloud/SETUP.md` (~150 LOC) — 45-min step-by-step setup guide (Supabase + Stripe + Cloudflare + Streamlit secrets)
- `entitlements.py` (~230 LOC) — Python klijent: TTL cache 30s, graceful degradation, `is_pro/get_checkout_url/record_download/get_last_chain_hash`
- `watermark.py` (~110 LOC) — per-doc forensic serial generator (sha256[:12] format `NN-NNNN-NNNNNN`), footer HTML free/pro, OOXML `dc:identifier` stamping
- `docx_export.py` proširen — opcijski `user_id, doc_type, plan` parametri; K3 watermark se aktivira kad je `doc_type` proslijeđen
- `LEGAL-SUITE.py` proširen — `entitlements.render_subscribe_cta()` u sidebar-u
- `stranice/tos.md` + `stranice/privacy_policy.md` (oba nacrt v1.0, **trebaju pravnički pregled prije Stripe live mode-a**)
- `stranice/pravila.py` — render kroz tabs; "Pravila i privatnost" stavka u sidebar Alati sekciji
- `tests/test_watermark.py` (12 testova) + `tests/test_entitlements.py` (8 testova)

**Korisničke off-line akcije za aktivaciju** (slijedi `cloud/SETUP.md`):
1. Supabase: create projekt → SQL Editor → paste `supabase_schema.sql` → run → kopiraj URL + anon + service_role keys
2. Stripe: account → Product `LegalTechSuite PRO` → kopiraj Price ID + Secret key (test mode prvi)
3. Cloudflare: `npm install -g wrangler && wrangler login && cd cloud && npm install && wrangler deploy` → kopiraj URL + 6 secrets
4. Streamlit Cloud: dodaj 3 secrets u Settings (`SUPABASE_URL`, `SUPABASE_ANON_KEY`, `STRIPE_CHECKOUT_URL_BASE`)

### K1 — Janusov audit lanac (zatvoreno P3 inline, 7 dana implementacije)

Rezultat ciklusa BRZ_MOZAK GLAVNI INZINJER LEGAL P2 hibrid:
- Agent 2A FILOZOF (subagent, prazan context) → 5 kristala iz Borgesa, Janusa, Wittgensteina, Maussa, srednjevjekovnih marginalija
- Agent 2B INŽENJER (inline) → kandidat **"Janusov audit lanac"** (Tip B kompozicija: SHA256 chain + Generator versioning registry + RFC 8785 JCS canonical form)
- Agent 3 UBOJICA (inline) → 7/7 prijedlog
- Agent 5 NEZAVISNI SUDAC (subagent, prazan context, bez UBOJICA score-a) → **7/7 USPJEH, delta 0, bez REFRAKTOR signala**

Implementacija isporučena:
- `audit_chain.py` (~120 LOC) — `canonical_input_hash` (RFC 8785 JCS + NFC Unicode), `compute_generator_hash`, `build_chain_link`, `verify_chain`, `compute_full_audit`
- `cloud/0007_audit_chain.sql` (14 LOC) — idempotent ALTER `download_log` ADD 6 kolona + 2 indeksa
- `entitlements.py` proširen — `record_download(audit=...)` opcijski K1 polja; `get_last_chain_hash()` helper
- `docx_export.py` proširen — `pripremi_za_docx(input_dict, generator_module_path)` opcijski parametri; `audit_chain.compute_full_audit` poziv prije `record_download`
- `scripts/build_generators_registry.py` (~70 LOC) — skenira `generatori/*.py`, sha256 source, piše `generators_registry.json` s git commit-om
- `scripts/replay_docx.py` (~95 LOC) — CLI s `<serial_hash>`, fetch `download_log`, ispiše replay upute
- `tests/test_audit_chain.py` (15 testova, ~170 LOC)
- `generators_registry.json` (auto-generiran) — 15 generatora indeksirano, git commit `aa1721e`

**Test status**: **194/194 cijela suite PASS** (179 baseline + 15 K1 = 194), 0 regresija.

**Audit log K1 ciklusa**:
- `GLAVNI_INZINJER/IDEJE/CIKLUS_K1_2026-04-27.md` — pun ciklusni log s 5 koraka i agentskim outputima
- `GLAVNI_INZINJER/IDEJE/Ideja_K1_Janusov_audit_lanac.md` — implementacijski plan + ROI tablica + 5 napada/pobijanja

### BRZ_MOZAK GLAVNI INZINJER LEGAL adaptacija (4 dokumenta, 1037 LOC)

Standalone repozitorij u `APLIKACIJA/GLAVNI_INZINJER/` jer RIJEKA_PRATILAC pravila v5 referenciraju ML primitive (OSNOVE_ARHITEKTURE.md) koji bi za LegalTech aktivirali AI Act high-risk + nadripisarstvo.

- `CLAUDE_LEGAL.md` (142 LOC) — entry point, modus P1/P2/P3, file index
- `LEGAL_ARHITEKTURA.md` (296 LOC) — **zamjena OSNOVE_ARHITEKTURE.md**. 5 domena: §I Kriptografski audit (SHA256/Merkle/RFC 3161 timestamps/content-addressing), §II PWA offline (Service Worker/IndexedDB/Web Crypto/manifest), §III Idempotentni webhooci (idempotency key/cron reconciliation/outbox pattern), §IV Schema versioning (registry/JSON Schema/expand-contract), §V Deterministic templating (pure functions/HTML→DOCX/OOXML/HR padeži). **Eksplicit eksluzija** svih AI/ML primitiva.
- `GLAVNI_INZINJER_PRAVILA_LEGAL_v1.md` (262 LOC) — **Pravila L1-L6** (LegalTech adaptirana 33-38). L1 misija (Tip A/B/C anti-AI), L2 input stack, L3 strikt kineski zid, L4 ROI metrike (DRM/AI Act margin/GDPR/MRR/latency/MTTR), L5 jedna ideja po ciklusu, **L6 dvostruki anti-drift** (anti-AI + anti-business). Mapa naslijeđenih pravila 1-32 iz RIJEKA_PRATILAC.
- `GLAVNI_INZINJER_AGENTI_LEGAL.md` (337 LOC) — P2 hibrid orkestrator protokol. Konkretni prompt templati za subagente (Filozof + Sudac). Sudac protokol s 7-test STRIKT (anti-AI grep u T7) + R1-R5 realnost-provjere.

### README.md (700+ linija)

Pun README za APLIKACIJA s **MONETIZACIJA** sekcijom (Supabase + Stripe + Cloudflare + Streamlit Cloud arhitektura + setup + trošak procjena 0 EUR fiksno) + AI Act 2026 + nadripisarstvo pozicija + Razvojni okoliš BRZ_MOZAK + roadmap K1-K4.

---

## Sljedeći prioriteti (po dogovoru s korisnikom)

### 0. K1.5 — UI ožičenje (PRIORITET 0, sad)

**Problem**: K1 audit chain je opt-in. `pripremi_za_docx()` ima nove parametre `input_dict` i `generator_module_path`, ali `stranice/*.py` ih trenutno **ne prosljeđuju**. Bez ovog koraka K1 je mrtvi kod u repu.

**Akcija**: rutinski refaktor (NE kroz BRZ_MOZAK — jasan task, nema arhitekturne odluke). Pun pregled svih `stranice/*.py` forma:

```python
# Trenutno:
doc_html = generiraj_kupoprodajni_ugovor(podaci)
prikazi_dokument(doc_html, "Kupoprodajni_ugovor.docx", "Preuzmi")

# Treba:
doc_html = generiraj_kupoprodajni_ugovor(podaci)
prikazi_dokument(
    doc_html,
    "Kupoprodajni_ugovor.docx",
    "Preuzmi",
    doc_type="ugovor",
    doc_subtype="kupoprodajni",
    input_dict=podaci,  # K1: dict s user input-ima
    generator_module_path="generatori/ugovori.py",  # K1: za bytecode hash
)
```

**Stranice koje treba ožičiti** (15 + jednostavni mod):
- `stranice/ugovori.py`, `tuzbe.py`, `ovrhe.py`, `zalbe.py`, `zemljisne.py`, `opomene.py`, `punomoci.py`, `trgovacko.py`, `obvezno.py`, `obiteljsko.py`, `upravno.py`, `kazneno.py`, `stecajno.py`, `potrosaci.py`, `posrednik_najam.py`, `jednostavno.py`

Trebat će i prilagoditi `pomocne.prikazi_dokument()` da prima novi `input_dict` parametar i prosljeđuje u `pripremi_za_docx`. Provjeri točno gdje se zove `pripremi_za_docx` — ako je preko `prikazi_dokument`, sve prolazi kroz jedan helper i refaktor je čišći.

**LOC procjena**: ~30 LOC po stranici × 16 stranica = ~480 LOC izmjena (ali tipično 5-10 LOC po pozivu, ne ekspanzija). **Solo dev rok**: ~2-3 dana ako se radi jedan-po-jedan; ~1 dan ako se napravi batch refactor s search/replace pattern-a.

**Test plan**: postojeći 194 testovi moraju ostati zeleni. Plus po jedan smoke test po stranici da generira docx s prosljeđenim `input_dict` i provjeri da se `download_log` red puni s K1 audit poljima (kad je Supabase konfiguriran; kad nije, tihi fall-through ostaje).

**Acceptance**: nakon K1.5, svaki docx download generira download_log red s popunjenim `input_canonical_hash`, `output_sha256`, `parent_hash`, `current_hash`, `generator_version_hash`, `input_schema_version`. Verify hash chain s `verify_chain([rows]) → (True, None)`.

### 1. K2 — PWA Service Worker + IndexedDB (P2 hibrid)

**Pokreće se NAKON K1.5**. Nije inline — pokreće se kao pun BRZ_MOZAK GLAVNI INZINJER LEGAL P2 hibrid ciklus jer Service Worker + IndexedDB arhitektura traži precizno planiranje da ne razbije postojeći deterministic kod.

**Cilj**: client-side docx generacija (umjesto server-side preko Streamlit Cloud), offline-first PWA, IndexedDB cache za korisnikove privatne podatke (auto-save). Smanjuje server trošak, omogućuje offline rad, povećava UX.

**Pokretanje**: kad K1.5 zatvoren, korisnik kaže "pokreni K2", orkestrator priprema `CIKLUS_K2_2026-XX-XX.md` s SEED + 5 kulturoloških izvora + izazov, slijedi P2 hibrid 7 koraka iz `GLAVNI_INZINJER_AGENTI_LEGAL.md`.

**Specifični izazov za Agent 1 IZVIDNIK**: Service Worker arhitektura (LEGAL_ARHITEKTURA §II.1), IndexedDB (§II.2), Web Crypto API (§II.3), PWA manifest (§II.4) — koji su najjača kompozicija za APLIKACIJA stack koji **mora ostati deterministic** (60+ generatora moraju nastaviti raditi)?

### 2. K1 faza 2 — Merkle root javno (BACKLOG)

**Ne implementira se sad**. U backlogu. Reaktivira se kad korporativni klijent izričito zatraži external auditability. Kriptografska arhitektura je definirana u `LEGAL_ARHITEKTURA.md §I.2` — daljnja implementacija slijedi taj okvir.

---

## Trenutno stanje stack-a

### Lokalno (dev)
- Streamlit lokalan: `streamlit run LEGAL-SUITE.py` → `http://localhost:8501`
- Pytest: `python -m pytest tests/ -q` → **194 passed in ~1s**
- Generators registry: `python -m scripts.build_generators_registry` → 15 generatora, git commit `aa1721e`

### Cloud (planirano, čeka korisnikov setup)
- Streamlit Community Cloud: već deployed na `https://{{STREAMLIT_APP_ID}}.streamlit.app`
- Supabase: čeka korisnikov account + SQL Editor run `cloud/supabase_schema.sql` + `cloud/0007_audit_chain.sql`
- Cloudflare Worker: čeka `wrangler deploy` (cloud/ folder spreman)
- Stripe: test mode account + product setup

---

## Što SE PRIPAZILO da se ne razbije

- **Backwards compatibility**: svi novi parametri u `pripremi_za_docx`, `record_download`, `html_u_docx` su opcijski s default `None`. Stari pozivi rade bez izmjene.
- **Graceful degradation**: ako Supabase ne odgovara, `entitlements` funkcije vraćaju free tier; ako `audit_chain` import fail-a, `docx_export` nastavlja s K3 watermarkom; ako `watermark` import fail-a, `docx_export` nastavlja bez forensic ID-a (samo K3 NACRT watermark).
- **Idempotent migrations**: `cloud/0007_audit_chain.sql` koristi `ADD COLUMN IF NOT EXISTS` (Postgres 9.6+); siguran ponovni run.
- **Feature-flag-less rollout**: K1 audit chain je opt-in preko parametara (ne env varijable), tako da nema "globalni flag" koji bi mogao razbiti pola app-a; svaki poziv se posebno opt-ina.

---

## Što sljedeća sesija mora znati

1. **194 pytest testovi pass** — verify s `python -m pytest tests/ -q` prije bilo kakve K1.5 izmjene
2. **`stranice/*.py` su 16 fajlova** koje treba refaktorirati — svaki ima `def render_X():` funkciju s formama → `if st.button("Generiraj")` blokovi → `prikazi_dokument(html, ...)` poziv
3. **`pomocne.prikazi_dokument()`** je vjerojatno glavni helper koji zove `pripremi_za_docx`. Ako je tako, jedan refaktor tamo + prosljeđivanje kroz parametre rješava sve stranice odjednom. **Provjeri prvo `pomocne.py`** prije batch refactor-a.
4. **`generators_registry.json` regenerira se pri svakom git push-u** kroz `scripts/build_generators_registry.py` — ako se to ne pokreće u CI/CD, dodaj kao GitHub Action ili pre-commit hook
5. **Auth migracija `.users.json` → Supabase Auth ostaje na čekanju** (ne K1.5; odvojen task). Trenutno `auth.py` koristi lokalni file koji je ephemeral na Streamlit Cloud — postoji ali ne blokira K1.5.

---

## Otvorena pitanja za sljedeću sesiju

1. **`pomocne.prikazi_dokument()` API**: ima li već parametre za `input_dict`/`generator_module_path` ili treba proširiti? (Nije provjereno tijekom K1 implementacije.)
2. **Jednostavni mod (`stranice/jednostavno.py`)**: koja struktura ima — generator-by-generator ili neki drugi pattern? Treba li poseban tretman?
3. **Posrednik najam (`stranice/posrednik_najam.py`)**: spomenuto u `_MODULI` ali ne u CLAUDE.md kategorizaciji — provjeriti je li ozičenje istog patterna.

---

## Geneza handoff-a

Nastalo: 2026-04-27 popodne. Korisnik je zatvorio K3 + K5 + K1 + BRZ_MOZAK adaptaciju u jednoj sesiji (~250k tokena), pa je tražio handoff prije clear context-a tako da sljedeća sesija (s "praznim" kontekstom) može nastaviti bez zbunjenosti.

**Sljedeća akcija (ne-trivijalna, ne kroz BRZ_MOZAK)**: K1.5 ožičenje 16 stranice fajlova s prosljeđivanjem `input_dict` u `pripremi_za_docx`. Cilj: K1 audit chain operativan end-to-end na svakom docx download-u.

**Acceptance test za K1.5**: pokreni jedan generator, provjeri da `download_log` red ima popunjena K1 polja (`input_canonical_hash`, `output_sha256`, `parent_hash`, `current_hash`, `generator_version_hash`, `input_schema_version`), pokreni `audit_chain.verify_chain(rows)` na svim redovima, dobiti `(True, None)`.
